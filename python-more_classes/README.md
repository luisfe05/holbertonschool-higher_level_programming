# Python - More Classes and Objects

## Files

| File | Description |
| :--- | :--- |
| `0-rectangle.py` | An empty `Rectangle` class definition. |
| `1-rectangle.py` | A `Rectangle` class with private `width` and `height` properties and validations. |
| `2-rectangle.py` | A `Rectangle` class with public methods `area` and `perimeter`. |
| `3-rectangle.py` | A `Rectangle` class with a `__str__` method to print the rectangle using `#`. |
| `4-rectangle.py` | A `Rectangle` class with a `__repr__` method to recreate instances via `eval()`. |
| `5-rectangle.py` | A `Rectangle` class with a `__del__` method that prints a deletion message. |
| `6-rectangle.py` | A `Rectangle` class with a `number_of_instances` class attribute tracker. |

## Author
Luis Gonzalez - Holberton School San Juan, Puerto Rico
